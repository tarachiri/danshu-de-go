# 2026-07-02 お知らせタブ安全修正メモ

## 概要

お知らせタブ改善の現状調査を受けて、まず安全に進められる表示品質の修正だけを実施した。

対象は `danshu-tools/generate_news.py`。DB書き込み、`news.json` 本番書き込み、収集対象拡大、`danshu_sites` 接続は行っていない。

## 実施した修正

### 1. 文字化けとソーバーねっとイベント名破損の修正

コミット: `2530d73 fix: お知らせ収集の文字化けとイベント名破損を修正`

変更内容:

- HTML/PDFページ解析を `r.text` 依存から `r.content` ベースに変更。
- 滋賀県断酒同友会、全断連サイトなどで発生していた文字化けを改善。
- ソーバーねっとイベント名の処理で、タイトル中の数字を削らないよう修正。

改善例:

- `第45回東北断酒学校` が `第回東北断酒学校` にならない。
- `令和8年度` が `令和年度` にならない。
- タイトル先頭の `土 ~ 日` などの曜日残骸が減る。

tyo側バックアップ:

- `/home/maji/danshu-tools/generate_news.py.bak-20260702-safe-news-fixes`

検証:

- `tyo` 上でサンプルイベント名の変換を確認。
- 滋賀・全断連のPDFタイトルが日本語として読めることを確認。
- `/home/maji/danshu-tools/generate_news.py --dry-run` 完走。
- `news.json` 本番書き込みなし。
- DB書き込みなし。

### 2. PDFタイトル品質改善

コミット: `a6e3eb1 fix: PDFタイトルの表示品質を改善`

変更内容:

- PDFリンクの見出しを表示向けに整える `clean_pdf_title()` を追加。
- 先頭の `>` / `＞` を除去。
- `★★★`、短い数字だけ、長いハッシュ風ファイル名など、利用者に意味が伝わりにくいタイトルを汎用名へ補正。
- 分かる範囲だけファイル名から安全に補助タイトルを作成。

改善例:

- `>神奈川県断酒連合会 沿革(PDF)` → `神奈川県断酒連合会 沿革(PDF)`
- `★★★` → `静岡県断酒会 PDF資料 (08yotei)`
- `07` → `dansyukai PDF資料`
- `danwa202511` → `断話 2025年11月号`
- `SBIRTS2022` → `SBIRTS 2022`
- `d87e2fc727362dfe8c724ca5fca98167-1` → `福岡県断酒連合会 PDF資料`

tyo側バックアップ:

- `/home/maji/danshu-tools/generate_news.py.bak-20260702-pdf-title-quality`

検証:

- `tyo` 上で代表例のタイトル変換を確認。
- PDF収集だけを一時ファイルで実行し、24件のPDFタイトル一覧を確認。
- `/home/maji/danshu-tools/generate_news.py --dry-run` 完走。
- `news.json` 本番書き込みなし。
- DB書き込みなし。

## 触っていないこと

- `danshu_sites` はまだ `generate_news.py` に接続していない。
- 未使用の `news_url` / `rss_url` はまだ有効化していない。
- 京都断酒平安会RSSのスパム対策はまだ未実装。
- PDFの巡回範囲は広げていない。トップページ等、既存の収集範囲のまま。
- 休会・中止・変更情報を `schedule_exceptions` に反映する処理は未実装。
- フロント側 `news-tab.js` のキャッシュ、表示順、URLエスケープは未修正。

## 次に安全に進めやすい候補

1. `news-tab.js` のURLエスケープとイベント表示順修正。
2. フロント側キャッシュの見直し。
3. `danshu_sites` 接続前のRSSスパムフィルタ設計。
4. PDF検出を `news_url` まで広げる設計。
5. 休会・中止・変更情報を `schedule_exceptions` に流すためのdry-run専用設計。

## 注意

`tyo` 側の `/home/maji/danshu-tools` には未追跡ファイルや変更済みファイルが多く、安易な `git pull` は避けた。今回は対象ファイルだけバックアップ付きで差し替えた。
